# Debug builds only: unoptimized frames under the async server + ACT proof
# call depth slightly exceed the default 2 MiB test-thread stacks. Release
# builds fit the defaults at D = 8 balance digits (at the crate's original
# D = 80 even release builds needed ~16 MiB).
[env]
RUST_MIN_STACK = "8388608"
